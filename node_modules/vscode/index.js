function file(f) {
  return { fsPath: f, path: f };
}

function parse(u) {
  return { fsPath: u, path: u, toString: function toString() { return u; } };
}

export var Uri = {
  file: file,
  parse: parse
};

function RelativePattern(base, pattern) {
  this.base = base;
  this.pattern = pattern;
}

export { RelativePattern };

function executeCommand() {
  return Promise.resolve();
}

function registerCommand() {
  return { dispose: function dispose() {} };
}

export var commands = {
  executeCommand: executeCommand,
  registerCommand: registerCommand
};

function showInformationMessage() {
  return Promise.resolve();
}

function showWarningMessage() {
  return Promise.resolve();
}

function showErrorMessage() {
  return Promise.resolve();
}

function createStatusBarItem() {
  return {
    show: function show() {},
    hide: function hide() {},
    dispose: function dispose() {}
  };
}

function registerWebviewViewProvider() {
  return { dispose: function dispose() {} };
}

function onDidCloseTerminal() {
  return { dispose: function dispose() {} };
}

function createWebviewPanel() {
  return {
    webview: {
      html: '',
      onDidReceiveMessage: function onDidReceiveMessage() {},
      asWebviewUri: function asWebviewUri(u) { return u; }
    },
    onDidDispose: function onDidDispose() {},
    reveal: function reveal() {},
    dispose: function dispose() {}
  };
}

function createTerminal(options) {
  return {
    name: options && options.name ? options.name : 'Terminal',
    sendText: function sendText() {},
    show: function show() {},
    dispose: function dispose() {}
  };
}

export var window = {
  showInformationMessage: showInformationMessage,
  showWarningMessage: showWarningMessage,
  showErrorMessage: showErrorMessage,
  createStatusBarItem: createStatusBarItem,
  registerWebviewViewProvider: registerWebviewViewProvider,
  onDidCloseTerminal: onDidCloseTerminal,
  createWebviewPanel: createWebviewPanel,
  createTerminal: createTerminal,
  activeTextEditor: null,
  tabGroups: { all: [] }
};

function getConfiguration() {
  return {
    get: function get(key, def) { return def; },
    update: function update() { return Promise.resolve(); }
  };
}

function onDidChangeConfiguration() {
  return { dispose: function dispose() {} };
}

function createFileSystemWatcher() {
  return {
    onDidChange: function onDidChange() { return { dispose: function dispose() {} }; },
    onDidCreate: function onDidCreate() { return { dispose: function dispose() {} }; },
    onDidDelete: function onDidDelete() { return { dispose: function dispose() {} }; },
    dispose: function dispose() {}
  };
}

function openTextDocument() {
  return Promise.resolve({ getText: function getText() { return ''; } });
}

export var workspace = {
  getConfiguration: getConfiguration,
  onDidChangeConfiguration: onDidChangeConfiguration,
  createFileSystemWatcher: createFileSystemWatcher,
  openTextDocument: openTextDocument,
  workspaceFolders: []
};

function getDiagnostics() {
  return [];
}

export var languages = {
  getDiagnostics: getDiagnostics
};

export var DiagnosticSeverity = { Error: 0, Warning: 1, Information: 2, Hint: 3 };
export var StatusBarAlignment = { Left: 1, Right: 2 };
export var ViewColumn = { One: 1, Two: 2, Three: 3 };
export var ConfigurationTarget = { Global: 1, Workspace: 2, WorkspaceFolder: 3 };
export var TerminalLocation = { Panel: 1 };

var vscodeMock = {
  Uri: Uri,
  RelativePattern: RelativePattern,
  commands: commands,
  window: window,
  workspace: workspace,
  languages: languages,
  DiagnosticSeverity: DiagnosticSeverity,
  StatusBarAlignment: StatusBarAlignment,
  ViewColumn: ViewColumn,
  ConfigurationTarget: ConfigurationTarget,
  TerminalLocation: TerminalLocation
};

export default vscodeMock;
